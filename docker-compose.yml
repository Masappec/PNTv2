version: '3'

services:

  pnt_app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8082:80"
    volumes:
      - .:/app

    networks:
      - pnt_network

    environment:
      - VITE_URL_API= http://pnt_network:8081

networks:
  pnt_network:
    driver: bridge
